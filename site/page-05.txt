enalty" min="1" max="1.5" step="0.01"></div></div>
<div class="settings-section"><h3>Thinking models</h3><div class="field"><label>Thinking mode</label><select id="thinkingMode"><option value="auto">Auto-detect</option><option value="on">Request thinking</option><option value="off">Request final answer only</option></select><div class="hint">Auto separates common reasoning formats without changing the model's chat template.</div></div><label class="check"><input id="showThinking" type="checkbox"> Expand thinking sections automatically</label><div class="field"><label>Reasoning kept in rolling context</label><select id="reasoningContext"><option value="final">Final answers only</option><option value="recent">Recent reasoning only</option><option value="all">All available reasoning</option></select><div class="hint">This controls what is sent back to the model in later turns.</div></div><div class="field" id="recentReasoningField"><label>Recent assistant turns with reasoning</label><input type="number" id="recentReasoningTurns" min="1" max="20" step="1"></div></div>
<div class="settings-section"><h3>Context</h3><div class="field"><label>Maximum response tokens</label><input type="number" id="maxTokens" min="16" max="4096" step="16"></div><div class="field"><label>Context length</label><input type="number" id="context" min="256" max="32768" step="128"><div class="hint" id="contextHint">Changing context length requires reloading a local model.</div></div><label class="check"><input id="rollingContext" type="checkbox"> Use rolling conversation context</label><div class="hint">Keeps the newest complete turns and drops the oldest when the model context is full.</div><div class="button-row"><button id="inspectContext">Inspect context sent to model</button></div></div>
<div class="settings-section"><h3>Runtime</h3><label class="check"><input id="useGpu" type="checkbox"> Use WebGPU when available <span class="small">(experimental)</span></label><div class="hint" id="gpuHint"></div><div class="field"><label>CPU threads</label><input type="number" id="threads" min="1" max="32"><div class="hint" id="threadHint"></div></div></div><div class="settings-section"><h3>System prompt</h3><div class="field"><textarea id="systemPrompt"></textarea></div></div><button class="primary" id="saveSettings">Save settings</button></div></aside>
<dialog id="errorDialog"><div class="dialog-head"><h3 id="errorTitle">Error</h3></div><div class="dialog-body"><pre id="errorText"></pre></div><div class="dialog-foot"><button class="primary" id="errorClose">Close</button></div></dialog>
<dialog id="aboutDialog"><div class="dialog-head"><h3>About PortableLM</h3></div><div class="dialog-body"><p>PortableLM is a browser application for local language-model chat.</p><p><strong>Privacy:</strong> local GGUF inference runs on the current device. Hugging Face mode downloads a selected model, then inference remains local. API mode sends prompts to the configured endpoint.</p><p><strong>Model packaging:</strong> no language model is embedded. Choose a local GGUF file or download a public GGUF from Hugging Face.</p><p>Inference engine: Wllama / llama.cpp. This hosted build loads version-pinned runtime assets.</p><pre id="environmentInfo"></pre></div><div class="dialog-foot"><button class="primary" id="aboutClose">Close</button></div></dialog>
</body></html>